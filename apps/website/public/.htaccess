Options -MultiViews
RewriteEngine On

# SPA fallback — API calls go directly to helferchen-api.onrender.com
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
