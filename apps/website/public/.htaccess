Options -MultiViews
RewriteEngine On

# SPA fallback — all routes handled by React app, API proxied via VPS
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
